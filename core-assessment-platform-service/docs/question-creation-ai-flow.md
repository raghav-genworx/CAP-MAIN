The whole app is already built by Codex, so use the existing project context, architecture, frontend components, backend APIs, database models, LangGraph workflow, AI services, Judge0/self-hosted execution service, and current Question Management implementation. Do not rebuild the app from scratch. First inspect the existing code, understand how the current question creation flow works, then refine and improve it carefully.

I want to refine the recruiter-side Question Management and New Question Creation flow for best AI quality, LangGraph reliability, CodeChef-style coding format, execution validation, and professional UI/UX.

Core requirement:
The platform should use CodeChef-style coding questions for now.

Candidate format:

* Candidate writes a complete program.
* Candidate reads input from STDIN.
* Candidate prints output to STDOUT.
* Do not use LeetCode-style function signatures or predefined function parameters for now.
* Candidate should not print prompts like `Enter number:`.
* The execution engine should run candidate code with testcase input and compare actual stdout with expected output.

Example candidate style:

```python
n = int(input())
arr = list(map(int, input().split()))
print(sum(arr))
```

Expected testcase format:

```json
{
  "input": "5\n1 2 3 4 5\n",
  "expected_output": "15\n",
  "is_sample": true,
  "explanation": "Sum of all elements is 15."
}
```

Question Management table changes:

1. Show `topics` in the questions table.
2. Show `tags/category` in the questions table.
3. Show `difficulty` if it already exists.
4. Difficulty, topics, and tags/category should not be manually selected in the early basic creation step.
5. Difficulty, topics, and tags/category should be generated by AI near the final stage after the problem, test cases, and reference solution are ready.

New Question Creation should be refined into this 5-step flow:

Step 1: Basic Details

* Keep existing basic details fields from the current app.
* Remove difficulty selection from this step.
* Remove tag/category selection from this step.
* Basic details are only for recruiter context, such as title/rough idea, role, topic hint, assessment context, experience level, or whatever fields already exist in the app.

From Step 1, recruiter should have two paths:

1. `Generate Whole Question with AI`
2. `Continue Manually`

When recruiter clicks `Generate Whole Question with AI`:

* Open a modal/popup.
* Show a clean summary of the basic details already entered.
* Provide one single textbox where recruiter can give extra prompt/instructions.
* This basic details summary + recruiter prompt should trigger the full LangGraph question generation workflow.
* The AI should generate the complete question, test cases, reference solution, validate them, classify metadata, and prepare final preview.

When recruiter clicks `Continue Manually`:

* Move to Step 2 where recruiter can type the problem statement and use AI only for specific fields if needed.

Step 2: Problem Statement
This step should contain:

1. Problem description
2. Input format
3. Input format explanation/description
4. Output format
5. Output format explanation/description
6. Constraints
7. Sample input
8. Sample output
9. Sample explanation

Recruiter should be able to manually type all sections.

Also provide AI assistance at field/section level:

* `Complete with AI`
* `Improve with AI`

Important behavior:

* If recruiter types only the problem description, AI should be able to complete input format, output format, constraints, sample input/output, and explanation.
* If recruiter types rough input format, AI should improve only input format and its explanation.
* If recruiter types rough constraints, AI should improve only constraints.
* If recruiter wants only output format completed, AI should generate only output format and output explanation.
* Do not force whole question generation when recruiter wants only one section generated.
* Each field-level AI action must use all already entered problem details as context.
* When recruiter chooses full problem generation, combine problem description, input format, output format, explanations, constraints, sample input, sample output, and sample explanation in one LLM call where possible to avoid wasting separate LLM calls.
* The generated problem must clearly explain what each input value represents, similar to CodeChef problem statements.
* The generated output format must clearly explain what should be printed.

Step 3: Test Cases and Reference Solution
This step should support both manual and AI-assisted creation.

At the top of this step, provide a clear toggle or segmented control:

1. `Generate with AI`
2. `Enter Manually`

Manual mode:

* Recruiter can manually add sample test cases.
* Recruiter can manually add hidden test cases.
* Recruiter can manually provide reference solution.
* Recruiter can still click `Validate using Execution Engine`.
* Recruiter should also have the option to generate only test cases with AI or only reference solution with AI, based on the manually entered problem statement.
* This is important: even if recruiter manually typed the problem statement in Step 2, they may want AI only for test cases and solution in Step 3.

AI mode:

* Use the existing LangGraph/Judge0 workflow.
* The workflow should generate test cases, generate reference solution, validate using the execution engine, and repair if needed.

Step 3 AI loop:

1. `TestCaseAgent` generates sample and hidden test cases.
2. `SolutionAgent` generates an optimized reference solution for one primary language first.
3. The reference solution must be a full CodeChef-style program using STDIN and STDOUT.
4. `ExecutionValidationNode` runs the reference solution against all generated test cases using Judge0/self-hosted execution.
5. Compare actual stdout with expected output.
6. Use safe normalization such as trimming trailing whitespace, but do not ignore meaningful formatting errors.
7. If validation fails, route to a repair decision.
8. If the solution is wrong, send failure details back to `SolutionAgent`.
9. If expected output or testcase is wrong/inconsistent, send failure details back to `TestCaseAgent`.
10. Repeat until all tests pass, or max retry count is reached.
11. After primary language solution passes, generate equivalent solutions for the other supported languages.
12. Validate other language solutions too if the current execution service supports them.

The UI should show this loop visually:

* Do not just show a spinner.
* Show a visual flow/progress representation for:

  * generating test cases
  * generating reference solution
  * running code execution validation
  * comparing outputs
  * repairing test cases or solution
  * validating again
  * completed
* Show single-line live commentary/status while the loop runs.

Example commentary:

* “Generating strong sample and hidden test cases…”
* “Creating optimized Python reference solution…”
* “Running solution against 12 test cases…”
* “Test case 4 failed. Checking whether the issue is solution logic or expected output…”
* “Repairing reference solution…”
* “All test cases passed successfully.”

Test case quality:

* Generate normal cases.
* Generate edge cases.
* Generate boundary cases.
* Generate tricky cases.
* Generate constraint-heavy cases where useful.
* Avoid duplicate weak test cases.
* Ensure expected output is deterministic and correct.
* Hidden test cases should be strong enough to catch hardcoded or naive solutions.

Step 4: Recruiter Validation

* Show the recruiter the generated or manually entered test cases and reference solution.
* Recruiter should be able to review:

  * sample test cases
  * hidden test cases
  * expected outputs
  * explanations
  * reference solution
  * execution result
  * runtime and memory if available
* Recruiter should be able to:

  * edit test cases
  * edit reference solution
  * regenerate test cases
  * regenerate solution
  * revalidate
  * approve
* Provide a clear `Validate Reference Solution` button.
* Show pass/fail per testcase.
* Show compilation error, runtime error, timeout, wrong answer, or accepted status clearly.
* Do not silently proceed if validation fails.
* If validation fails, show a warning and require recruiter action.
* Only allow smooth movement to the next step when validation is successful, unless recruiter explicitly chooses to continue with warning.

Step 5: AI Metadata Classification

* Difficulty, topics, and tags/category should be generated by AI only at this stage.
* Recruiter should not select difficulty/tags/category in Step 1.
* `MetadataClassifierAgent` should generate:

  * difficulty: Easy / Medium / Hard
  * topics
  * tags/category
* Classification should use:

  * final problem statement
  * input/output format
  * constraints
  * test cases
  * reference solution
  * time complexity
  * space complexity
  * solution approach
* Show the generated metadata to recruiter for review.
* If the existing app supports override, keep it as an optional final override after AI classification, not as the primary creation flow.

Final Add to Question

* Show a full final preview before saving.
* Preview should include:

  * title
  * problem description
  * input format
  * input format explanation
  * output format
  * output format explanation
  * constraints
  * sample input
  * sample output
  * sample explanation
  * hidden testcase count
  * reference solution
  * supported language solutions
  * validation status
  * difficulty
  * topics
  * tags/category
* Provide final `Add Question` or `Save Question` button.
* Save question, test cases, reference solutions, validation result, difficulty, topics, and tags/category to the database.

LangGraph workflow refinement:
Use or refine the existing graph. Do not duplicate graph logic unnecessarily.

Required nodes:

1. `StartNode`

* Collect basic details, recruiter prompt, current manual fields, and generation mode.

2. `ProblemSpecAgent`

* Supports full problem generation and field-level generation.
* Full generation should produce:

  * title
  * problem description
  * input format
  * input explanation
  * output format
  * output explanation
  * constraints
  * sample input
  * sample output
  * sample explanation
* Field-level generation should only complete or improve the requested field using existing fields as context.

3. `TestCaseAgent`

* Generate sample and hidden test cases.
* Generate strong test cases.
* Return structured JSON only.

4. `SolutionAgent`

* Generate optimized reference solution.
* First generate for one primary language.
* Code must be a full STDIN/STDOUT program.
* Include time complexity and space complexity if supported by existing schema.

5. `ExecutionValidationNode`

* Run the reference solution using Judge0/self-hosted execution.
* Compare stdout and expected output.
* Capture accepted, wrong answer, compilation error, runtime error, timeout, memory limit, and other execution statuses.

6. `RepairDecisionNode`

* Decide whether the issue is:

  * wrong solution
  * wrong expected output
  * invalid testcase
  * weak testcase set
  * invalid problem format
  * execution failure

7. `RepairLoop`

* Route back to SolutionAgent or TestCaseAgent.
* Keep retry count.
* Stop at max retries.
* Send clear status to frontend.

8. `MultiLanguageSolutionAgent`

* After primary solution is validated, generate solutions for the other supported languages.
* All language solutions must follow CodeChef-style STDIN/STDOUT.
* Validate them if execution support exists.

9. `MetadataClassifierAgent`

* Generate difficulty, topics, and tags/category at the final step.

10. `FinalReviewNode`

* Build final preview object for frontend.

11. `SaveNode`

* Save approved question and related data.

AI output requirements:

* Use structured JSON wherever possible.
* Validate AI output before saving.
* Use existing validation system such as Pydantic/Zod/schema validation depending on the current app.
* Do not directly trust raw LLM text for database writes.
* Handle invalid JSON gracefully and retry or show useful error.

Backend requirements:

* Reuse existing backend structure and services.
* Extend existing APIs only where needed.
* Add/update database models or migrations only if required.
* Keep existing working flows intact.
* Add robust error handling for:

  * LLM failure
  * invalid AI response
  * missing required fields
  * Judge0 failure
  * compilation error
  * runtime error
  * timeout
  * wrong answer
  * max retry reached
* Add useful logs for debugging the generation and validation workflow.

Frontend/UI/UX requirements:

* Take maximum care with UI/UX.
* The flow should feel polished, professional, and recruiter-friendly.
* Use a clean 5-step wizard/stepper.
* Make the manual path and AI path very clear.
* Make field-level AI assistance easy to discover but not noisy.
* Do not make the UI feel like a raw developer/debugging tool.
* Use clear button hierarchy.
* Use clean spacing, typography, section grouping, cards, and validation states.
* Prevent accidental loss of entered content when moving between steps.
* Allow recruiter to edit AI-generated content before saving.
* Show loading states for AI generation, execution validation, repair loop, and metadata classification.
* Show status states clearly:

  * pending
  * running
  * passed
  * failed
  * warning
  * completed
* Step 3 must have a visual LangGraph/execution loop representation with one-line commentary.
* Final preview should be easy to scan and review.

Important final instruction:
The current app already has many of these pieces implemented and working. Your job is to inspect, check, refine, connect missing pieces, improve reliability, and improve UI/UX quality. Do not create duplicate pages, duplicate services, duplicate graph flows, or unnecessary rewrites.

After completing the changes, provide:

1. Summary of modified files
2. Backend changes
3. Frontend changes
4. LangGraph/AI workflow changes
5. Execution/Judge0 validation changes
6. Database/migration changes if any
7. How to test the full question creation flow
8. Any assumptions or limitations
