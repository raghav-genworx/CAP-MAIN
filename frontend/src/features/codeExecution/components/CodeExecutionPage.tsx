import { PageHeader } from "../../../components/common/PageHeader";

export function CodeExecutionPage() {
  return (
    <>
      <PageHeader
        title="Code Execution"
        description="Submit, monitor, and troubleshoot candidate code execution jobs."
      />
      <p>Execution workflows will live inside the codeExecution feature module.</p>
    </>
  );
}
